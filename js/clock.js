// clock.js
// The following comments are for JSLint

/*jslint nomen: true, white: true, vars: true */
/*global document, window, screen, console, Image, AQ */

PS = ( function ( ps )
{
	"use strict";

	var _sys = ps._sys = ps._sys || {}; // establish _sys

	_sys.clock = {
		// CONSTANTS

		// VARIABLES

		active : false // true if clock is active

		// METHODS

	};

	return ps;
} ( PS ) );

